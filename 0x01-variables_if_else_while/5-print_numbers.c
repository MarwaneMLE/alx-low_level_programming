#include <stdio.h>
/**
 * main:  prints all single digit numbers from 0 to 10.
 * Return: 0 if success
 */
int main(void)
{
	int i;

	for(i = 0; i <= 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
